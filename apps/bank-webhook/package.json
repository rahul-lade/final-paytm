{
  "name": "bank-webhook",
  "version": "1.0.0",
  "description": "Simulated bank webhook server",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx esbuild ./src/index.ts --bundle --platform=node --outfile=dist/index.js",
    "start": "node dist/index.js",
    "dev": "npm run build && npm run start"
  },
  "dependencies": {
    "@repo/db": "workspace:*",
    "@repo/validators": "workspace:*",
    "express": "^4.21.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0"
  }
}